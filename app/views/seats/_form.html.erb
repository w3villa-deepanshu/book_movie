<%= form_with(model: seat) do |form| %>
  <% if seat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
        <% seat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :seat_no, style: "display: block" %>
    <%= form.text_field :seat_no %>
  </div>

  <div>
    <%= form.label :booked, style: "display: block" %>
    <%= form.check_box :booked %>
  </div>

  <div>
    <%= form.label :show_id, style: "display: block" %>
    <%= form.text_field :show_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
